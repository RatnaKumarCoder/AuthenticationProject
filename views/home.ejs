<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>
        <%= title %>
    </title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/noty/3.1.4/noty.min.css">
    <script src="https://cdnjs.cloudflare.com/ajax/libs/noty/3.1.4/noty.min.js"></script> 
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js" integrity="sha384-C6RzsynM9kWDrMNeT87bh95OGNyZPhcTNXj1NW7RuBCsyN/o0jlpcV8Qyq46cDfL" crossorigin="anonymous"></script>
    <link rel="stylesheet" href="/css/home.css">
</head>
<body>
    <nav class="d-flex flex-row-reverse fixed-top">
        <a href="/sign-out" class="mx-4 my-2">Sign Out</a>
    </nav>
    <div id="pass_container" class="d-flex flex-column justify-content-center align-items-center">
        <h1 id="main-heading">Welcome!!! <%= user.name %> </h1>
        <div id="pass_div" class="d-flex flex-column justify-content-center align-items-center">
            <form action="/user/reset-password" method="post" class="d-flex flex-column justify-content-center align-items-center">
                <h1>Password Reset</h1>
                <input type="hidden" class="my-2" bvalue="<%= user.id %>" name="userId">
                <input type="password" class="my-2" name="password" placeholder="Enter old Password" />
                <input type="password" class="my-2" name="new_password" placeholder="Enter new Password" />
                <input type="password" class="my-2" name="confirm_password" placeholder="Re-enter new Password" />
                <button type="submit" class="my-2">Change Password</button>
            </form>
        </div>
    </div>
    <script>
        // diaplaying the noty.js notification
        <% if(flash.success && flash.success.length>0) {%>
            new Noty({
                theme: 'relax',
                text: '<%= flash.success %>',
                type: 'success',
                layout: 'topRight',
                timeout: 2000
            }).show();
        <%}%>
    
        <% if(flash.error && flash.error.length>0) {%>
            new Noty({
                theme: 'relax',
                text: '<%= flash.error %>',
                type: 'error',
                layout: 'topLeft',
                timeout: 2000
            }).show();
        <%}%>
    
    </script>
</body>
</html>